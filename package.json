{
  "name": "sample-game-spinner-winner",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "b:dist": "webpack --env=prod --config config/webpack.config.js --progress --profile --colors",
    "watch": "webpack --env=prod --config config/webpack.config.js --progress --profile --colors --watch",
    "server": "node ./run-server.js",
    "start": "start npm run watch && start npm run server",
    "osx": "npm run watch & npm run server"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com:gameeapp/sample-game-spinner-winner.git"
  },
  "author": "Entity Black",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.3"
  },
  "devDependencies": {
    "babel-core": "^6.24.1",
    "babel-loader": "^7.0.0",
    "babel-preset-env": "^1.5.1",
    "webpack": "^2.6.1"
  }
}
